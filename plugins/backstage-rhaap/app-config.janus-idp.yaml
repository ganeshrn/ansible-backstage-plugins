dynamicPlugins:
  frontend:
    ansible.plugin-backstage-rhaap:
      appIcons:
        - name: AnsibleLogo
          importName: AnsibleLogo
      dynamicRoutes:
        - path: /ansible
          importName: AnsiblePage
          menuItem:
            icon: AnsibleLogo
            text: Ansible
